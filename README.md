# oonjai-core-service

To install dependencies:

```bash
bun install
```

To run with debug ui:

```bash
DEBUG=true bun serve
```

This project was created using `bun init` in bun v1.2.19. [Bun](https://bun.com) is a fast all-in-one JavaScript runtime.
